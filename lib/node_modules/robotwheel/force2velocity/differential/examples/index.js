var discreteUniform = require( '@stdlib/random/base/discrete-uniform' );
var mecanum_wheeled = require( './../lib' );

var wheelbase = discreteUniform( 1, 10 );
var maximum_wheel_velocity = discreteUniform( 0, 50 );
var stall_torque = discreteUniform( 0, 10 );
var fx = discreteUniform( 0, 5 );
var fy = discreteUniform( 0, 5 );
var mz = discreteUniform( 0, 5 );

wheel_velocity = mecanum_wheeled( wheelbase, maximum_wheel_velocity, stall_torque, fx, fy, mz );
console.log( "Velocity of wheel 1: " + wheel_velocity[ 0 ] + " rad/s" );
console.log( "Velocity of wheel 2: " + wheel_velocity[ 1 ] + " rad/s" );